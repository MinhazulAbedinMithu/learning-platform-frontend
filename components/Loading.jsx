import React from "react";

const Loading = () => {
  return (
    <div className="h-screen w-screen flex items-center justify-center">
      <h4 className="text-cyan-500">Loading</h4>
    </div>
  );
};

export default Loading;
